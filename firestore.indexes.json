{
  "indexes": [
    {
      "_comment": "My Experiments / Trash: where ownerId == me && trash == ? orderBy updatedAt desc",
      "collectionGroup": "experiments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "trash", "order": "ASCENDING" },
        { "fieldPath": "updatedAt", "order": "DESCENDING" }
      ]
    },
    {
      "_comment": "Recent: where visibility == 'public' && trash == false orderBy createdAt desc",
      "collectionGroup": "experiments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "visibility", "order": "ASCENDING" },
        { "fieldPath": "trash", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "_comment": "Showcase home: where sourceType == 'video' && ownerId == 'system' orderBy createdAt desc",
      "collectionGroup": "experiments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "sourceType", "order": "ASCENDING" },
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "_comment": "Related: where recordingId == X && visibility in [public,unlisted]",
      "collectionGroup": "experiments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "recordingId", "order": "ASCENDING" },
        { "fieldPath": "visibility", "order": "ASCENDING" }
      ]
    },
    {
      "_comment": "Raw: where ownerId == me && isRaw == true (isRaw boolean mirrors segments==null, since composite indexes can't equality-match an array field)",
      "collectionGroup": "experiments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerId", "order": "ASCENDING" },
        { "fieldPath": "isRaw", "order": "ASCENDING" }
      ]
    },
    {
      "_comment": "Admin moderation queue: where status == 'open' orderBy createdAt desc",
      "collectionGroup": "streetviewReports",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "_comment": "Reports on one panorama, newest first (admin detail + closing them out on delete)",
      "collectionGroup": "streetviewReports",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "svId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "_comment": "Admin: panoramas auto-hidden by reports, newest first (also the bulk-restore list after a report-bombing)",
      "collectionGroup": "streetviews",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "hiddenByReports", "order": "ASCENDING" },
        { "fieldPath": "hiddenAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": [
    {
      "_comment": "Settings profile card: count comments authored by me across all experiments (collectionGroup query on senderId)",
      "collectionGroup": "comments",
      "fieldPath": "senderId",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "My Classes: find a student's joined classes via collectionGroup('members') where uid == me",
      "collectionGroup": "members",
      "fieldPath": "uid",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "Account deletion: a user's submissions across every class (collectionGroup('submissions') where studentUid == me) — including classes they have since left, where the membership doc is gone but the submission remains",
      "collectionGroup": "submissions",
      "fieldPath": "studentUid",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "Account deletion: a user's grades across every class (collectionGroup('grades') where studentUid == me)",
      "collectionGroup": "grades",
      "fieldPath": "studentUid",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "Account deletion: a user's private workspace items across every class (collectionGroup('workspace') where studentUid == me). The COLLECTION entry preserves the automatic single-field index the class page's own workspace query relies on — a field override replaces automatic indexing for that field",
      "collectionGroup": "workspace",
      "fieldPath": "studentUid",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "Account deletion: work promoted to a class showcase that belongs to a user (collectionGroup('showcase') where ownerUid == me)",
      "collectionGroup": "showcase",
      "fieldPath": "ownerUid",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    },
    {
      "_comment": "Account deletion: entries in OTHER users' block lists that point at the account being deleted (collectionGroup('blocks') where authorId == me). Without this the deleted account's id would survive as a bare string in strangers' block lists",
      "collectionGroup": "blocks",
      "fieldPath": "authorId",
      "indexes": [
        { "order": "ASCENDING", "queryScope": "COLLECTION" },
        { "order": "ASCENDING", "queryScope": "COLLECTION_GROUP" }
      ]
    }
  ]
}
