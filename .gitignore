# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Firebase service-account keys — never commit
serviceAccount*.json
*-firebase-adminsdk-*.json
.secrets/

# Firebase CLI deploy cache
.firebase/

# Migration runtime artifacts (Firestore baseline dump, logs, frame-restore progress)
scripts/.baseline/

# Storage audit artifacts (bucket listings). Point-in-time snapshots produced by ad-hoc
# Admin-SDK listings during the recordings/ ownership-ledger audit; 66MB+ and regenerable
# only as a NEW snapshot, never as the old one. Keep them on disk, out of the repo.
recordings-inventory-*.txt

# Digital-twin model bake-off runs (scripts/evalTwinScene.ts): frames + model answers + report.html
eval-twin/
